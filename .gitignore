node_modules
dist/
.env
.DS_Store
.vercel
*.env.local
# Arquivos de metadados do macOS
._*
# Service account do Firebase (segredo — nunca commitar)
serviceAccountKey.json
# Arquivo de importação de usuários (contém hashes — não commitar)
users-import.json
# Config local do Claude Code (por máquina)
.claude/settings.local.json

# Backups locais do Firestore (contêm dados — não commitar)
backups/
.env*
